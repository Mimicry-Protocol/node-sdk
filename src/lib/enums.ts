export enum ChainId {
  POLYGON_POS = 137,
  POLYGON_MUMBAI = 80001,
}

export enum CurrencySymbol {
  USD = 'usd',
  WETH = 'weth',
  USDC = 'usdc',
}

export enum MetricType {
  MARKET_CAP = 'marketcap',
}
